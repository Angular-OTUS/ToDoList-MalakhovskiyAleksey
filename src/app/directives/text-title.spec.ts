import { TextTitle } from './text-title';

describe('TextTitle', () => {
  it('should create an instance', () => {
    const directive = new TextTitle();
    expect(directive).toBeTruthy();
  });
});
